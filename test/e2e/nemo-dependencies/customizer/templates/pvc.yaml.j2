apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: finetuning-ms-models-pvc
  namespace: {{ namespace }}
spec:
  storageClassName: {{ '"local-path"' if localPathProvisioner.enabled else '""' }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
